# LocalStorageHook React App

This project is a simple React application created using `create-react-app`. It includes a custom hook, `useLocalStorage`, for managing state that persists in local storage.

## Project Structure